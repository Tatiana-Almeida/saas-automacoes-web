[tool.ruff]
line-length = 88
select = ["E", "F", "W", "C", "B", "I", "T" ]
# Removed `extend-ignore` that listed `W503` which older/newer ruff
# versions may not recognize. Use project-specific ignore in ruff config
# if necessary.

[tool.black]
line-length = 88
target-version = ["py311"]

[tool.isort]
profile = "black"
