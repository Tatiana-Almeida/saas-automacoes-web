# Example environment variables for local development
# Copy this file to .env and edit values as needed. Do NOT commit secrets.

#### Backend (Django)
# Use a Postgres DB URL. Example uses Docker service names from docker-compose.
DATABASE_URL=postgres://postgres:postgres@postgres:5432/saas_db
# Redis URL (use dedicated DB index for tests, e.g. /9)
REDIS_URL=redis://redis:6379/1
# Django secret key (development only)
SECRET_KEY=replace-me-with-dev-key
# Debug mode (True/False)
DEBUG=True
# Hosts allowed (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1
# Settings module to use (default for Docker dev)
DJANGO_SETTINGS_MODULE=saas_backend.settings

#### Postgres credentials (useful if your scripts reference them separately)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=saas_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

#### Frontend (Vite)
# Base API URL used by the frontend during development
VITE_API_URL=http://localhost:8000/api
# Optional: tenant host to prefill in dev
VITE_TENANT_HOST=empresa.localhost

#### Optional: CI / Snyk
# SNYK_TOKEN=your-snyk-token-here  # set as secret in CI, do not store locally
