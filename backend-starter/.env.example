# Django
DEBUG=True
SECRET_KEY=dev-insecure-key-change-me
ALLOWED_HOSTS=127.0.0.1,localhost
DJANGO_SETTINGS_MODULE=starter.settings.dev

# Database (choose one):
# Option A: Single URL (recommended for production)
# Examples:
#   Postgres: postgres://USER:PASSWORD@HOST:PORT/DBNAME
#   Postgres+SSL: postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
DATABASE_URL=

# Option B: Discrete vars (used if DATABASE_URL is empty)
DB_NAME=saas
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=postgres
DB_PORT=5432

# CORS
# Comma-separated list of origins (sem espaços)
# Ex.: http://localhost:3000,http://127.0.0.1:5173
CORS_ALLOWED_ORIGINS=http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000

# JWT lifetimes
# Minutos para o access token e dias para o refresh token
ACCESS_TOKEN_LIFETIME_MIN=15
REFRESH_TOKEN_LIFETIME_DAYS=7

# Production overrides example
# SECURE_SSL_REDIRECT=True
# SECURE_HSTS_SECONDS=31536000

# Email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=localhost
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@example.com

# Password reset
SITE_NAME=SaaS Automacoes
PASSWORD_RESET_BASE_URL=http://localhost:3000/reset-password

# Payment Providers
# Stripe
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# PayPal
PAYPAL_MODE=sandbox  # or 'live' for production
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here

# Automations
AUTOMATIONS_DRY_RUN=True
# Example WhatsApp API placeholders (used by services when available)
WHATSAPP_API_URL=
WHATSAPP_TOKEN=

# Celery / Redis
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=
CELERY_TASK_ALWAYS_EAGER=False

# DRF throttling
DRF_THROTTLE_RATE_ANON=50/min
DRF_THROTTLE_RATE_USER=200/min
DRF_THROTTLE_RATE_LOGIN=10/min

# Cache middleware TTL (seconds; set >0 to enable)
CACHE_MIDDLEWARE_SECONDS=0

# Security headers (enable in production)
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False
X_FRAME_OPTIONS=DENY
SECURE_REFERRER_POLICY=same-origin

# Logging
LOG_LEVEL=INFO
SECURITY_LOG_LEVEL=INFO
